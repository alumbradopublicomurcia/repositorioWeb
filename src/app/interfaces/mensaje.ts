export interface Mensaje {
    //nombre?: string;
    mensaje?: string;
    fecha?: string; // Date o string
    uid?: string; // Id del usuario.
    idIncidencia?: string; // Id de la incidencia a la que hace referencia.
}